import { ETheme } from "../types/core";
import type { Tab } from "../types/core";

export const THEMES_TABS: Tab[] = [
    { label: 'Light', id: ETheme.LIGHT },
    { label: 'Dark', id: ETheme.DARK },
];
